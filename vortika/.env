# General Notes
# Do not rename any of these fields.

# Follow <protocol>://<subdomain>.<domain>.<path>
# If using specific ports, specify these using :<port>
PREVIEW_DOMAIN='http://preview.localhost:3001'

# Follow <protocol>://<subdomain>.<domain>.<path>
# If using specific ports, specify these using :<port>
LIVE_DOMAIN='http://localhost:3001'

# Used to encrypt JWT tokens which are used as credentials.
# Ensure you meet the following industry standards criteria:
# - The key should be generated using a cryptographically secure random number generator.
# - Aim for a key length that aligns with the cryptographic algorithm you are using. For example, a minimum of 128 bits for symmetric encryption like AES-128, or 256 bits for AES-256.
# - Consider using a mix of uppercase letters, lowercase letters, numbers, and special characters in the key to increase its entropy. 
SECRET_KEY='3zxcAkWixF0fF2TFSNwjwEKYQGAVoQF8hAoqrbjIuJ56oHeddU'

MONGO_DB_URL='mongodb://127.0.0.1:27017/database1'

CORS_ALLOWED_ORIGINS=['http://localhost:5137']

NODE_ENV='development'